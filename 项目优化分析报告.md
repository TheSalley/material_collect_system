# 素材收集系统 — 项目优化分析报告

> 更新日期：2026-09-15
> 项目规模：src 下 127 个源文件（Vue 组件约 90 个、JS 约 37 个）
> 技术栈：Vue 3.5 + Vite 7 + Element Plus 2.11 + Tailwind CSS 4 + Pinia 3 + Vue Router 4 + ECharts 6 + Quill 2

---

## 一、已完成优化项（2026-09-14 ~ 2026-09-15）

### 1.1 代码质量清理

| # | 事项 | 详情 |
|---|---|---|
| 1 | 乱码修复 | `src/apis/file.js` 9 处 JSDoc 注释、`List.vue` 错误提示、`ModuleMode.vue` 上传提示全部修复 |
| 2 | 调试日志清理 | 12 处 `console.log` 全部清理，当前 src 下 **0 处残留** |
| 3 | 死代码清理 | 删除 `NavigationBar.vue`（未挂载）、`tailwindcss.config.js`（v4 冗余），清理 `layout/index.vue` 注释代码和死样式（67→25 行） |

### 1.2 架构重构

| # | 事项 | 详情 |
|---|---|---|
| 3 | 路由拆分 | 新建 `adminRoutes.js`（7 路由）、`customerRoutes.js`（7 路由）、`whitelist.js`（白名单工具），`index.js` 精简为入口+守卫 |
| 4 | 路由 order 参数 | 所有子路由添加 `meta.order`，`SideBar.vue` 按 order 升序排序 |
| 5 | 目录迁移 | `components/Field/` → `components/ElementorFields/`（8 子目录 56 个组件），`imageWp.vue` → `ImageWp.vue` |
| 6 | Pinia store 拆分 | 新建 `stores/user.js`（用户信息）、`stores/website.js`（站点信息），`stores/global.js` 改为兼容层 |

### 1.3 UI 统一

| # | 事项 | 详情 |
|---|---|---|
| 7 | 设计 token 体系 | 重写 `src/style.css`，`@theme` 定义完整 token（主色 #2b7cee/语义色/表面色/边框/圆角/阴影/间距），`:root` 覆盖 Element Plus CSS 变量 |
| 8 | 公共组件抽取 | 新建 `PageContainer.vue`、`PanelCard.vue`、`StatCard.vue` 三个布局组件 |
| 9 | 14 个页面 UI 迁移 | admin（List/UserList/Detail/Dashboard/MediaLibrary）+ customer（SiteInfo/Pages/Instruction/ProductList/ProductUpload/NewsList/NewsUpload）全部迁移，移除 40+ 处 `dark:` 手动暗色变体 |
| 10 | 节假日 API 集成 | 新建 `apis/holiday.js`，Dashboard 添加节假日倒计时面板 |

### 1.4 稳定性修复

| # | 事项 | 详情 |
|---|---|---|
| 11 | API 层统一错误处理 | 重写 `utils/http.js`，`baseRequest()` 统一处理超时/网络错误/HTTP 错误/401，**修复原有 401 bug**（原 `!res.ok` 先拦截导致 401 分支不可达），新增 `request()` 规范化返回 `{success,data,message,code}` |
| 12 | 8 个 API 文件简化 | auth/blacklist/file/media/proxy/site/translate/user 移除 `getAuthHeaders` 手动传参，`fetchWithAuth` 内部自动加 token |
| 13 | 路由守卫完善 | **修复角色校验 bug**（原条件永远不触发），新增 403 提示、redirect 回跳（`?redirect=原路径`），`login.vue` 支持回跳 |

### 1.5 ModuleMode.vue 拆分（核心重构）

原始 1989 行 → 最终 **328 行（-84%）**，分六个阶段渐进式拆分：

| 阶段 | 产物 | 行数减少 |
|---|---|---|
| 1. 工具函数 | `utils/moduleModeUtils.js`（15 个纯函数） | -413 |
| 2. useModuleParts | `composables/useModuleParts.js`（板块高亮+截图状态） | -89 |
| 3. useModuleUpload | `composables/useModuleUpload.js`（上传弹窗+媒体库） | -103 |
| 4. useDemoConfig | `composables/useDemoConfig.js`（Demo 配置，40+ 函数，最复杂） | -550 |
| 5. 子组件拆分 | `module-mode/PreviewPanel.vue` / `EditorPanel.vue` / `UploadDialog.vue` | -156 |
| 6. 样式拆分 | 样式下沉到子组件，`.pretty-scroll` 提升为全局样式 | -350 |

**最终文件结构**：
```
src/
├── components/
│   ├── ModuleMode.vue          # 328 行（纯容器）
│   ├── DataExtractor.vue       # 动态字段编辑
│   └── module-mode/
│       ├── PreviewPanel.vue    # 左侧板块预览
│       ├── EditorPanel.vue     # 右侧折叠编辑
│       └── UploadDialog.vue    # 上传弹窗
├── composables/
│   ├── useModuleParts.js       # 板块高亮+截图状态
│   ├── useModuleUpload.js      # 上传弹窗+媒体库逻辑
│   ├── useDemoConfig.js        # Demo 配置管理（40+函数）
│   └── useMedia.js             # 媒体库查询
└── utils/
    └── moduleModeUtils.js      # 15 个纯工具函数
```

---

## 二、待优化项（按优先级）

### P0 — 高优先级（影响全局一致性/可维护性）

#### 2.1 API 层迁移到新 `request()` 方式

- **现状**：57 处仍使用 `fetchWithAuth`，返回后端原始格式 `{code, data, message}`
- **目标**：逐步改用新的 `request()`，返回规范化格式 `{success, data, message, code}`
- **影响**：全局 API 调用行为一致性，调用方无需判断 `res.code === 0`
- **工作量**：中（8 个 API 文件，约 57 个函数，需逐个修改调用方）

#### 2.2 globalStore 兼容层退役

- **现状**：26 处仍引用 `useGlobalStore`，`stores/global.js` 作为兼容层组合 user/website 两个 store
- **目标**：逐步替换为 `useUserStore` / `useWebsiteStore`，最终删除 `stores/global.js`
- **影响**：减少一层间接调用，持久化 key 从 "global" 变为 "user"/"website"（升级后首次访问需重新登录）
- **工作量**：中（26 个文件，逐个替换 import 和使用方式）

---

### P1 — 中优先级（安全/可维护性/一致性）

#### 2.3 token 持久化安全

- **现状**：Pinia `persist:true` 整库明文序列化到 localStorage，token 明文存储
- **目标**：仅持久化非敏感字段（用户信息、站点信息），token 存内存或加密存储
- **影响**：安全性提升，防止 XSS 窃取 token
- **工作量**：小（修改 `stores/user.js` 的 persist 配置）

#### 2.4 ~~死代码清理~~ ✅ 已完成

- **已完成**：删除 `NavigationBar.vue`（未挂载）、`tailwindcss.config.js`（v4 冗余），清理 `layout/index.vue` 注释代码和死样式

#### 2.5 ElementorFields 56 个组件样式统一

- **现状**：`components/ElementorFields/` 下 8 子目录 56 个字段组件，样式可能不统一（不同开发者/不同时期编写）
- **目标**：统一字段组件的布局、间距、标签样式、输入框样式，接入设计 token
- **影响**：编辑区视觉一致性提升
- **工作量**：大（56 个组件，需逐个检查和调整）

#### 2.6 数据口径统一：customUpload 与 confirmSelectMedia 的 page 字段

- **现状**：`customUpload` 把素材 `page` 字段写成模块 elementor id，而 `confirmSelectMedia` 写页面标识，两处口径不一致
- **目标**：统一 `page` 字段含义，明确是页面标识还是模块 id
- **影响**：数据一致性，避免后续查询混乱
- **工作量**：小（修改 `useModuleUpload.js` 中两处逻辑）

---

### P2 — 低优先级（开发体验/质量保障/性能）

#### 2.7 添加 lint/format 脚本

- **现状**：`package.json` 已有 `eslint ^9.39.1` 和 `prettier ^3.6.2` 依赖，但无 `lint`/`format` 脚本
- **目标**：添加 `"lint": "eslint src --ext .vue,.js"`、`"format": "prettier --write src/**/*.{vue,js}"` 脚本，配置 `.eslintrc` 和 `.prettierrc`
- **影响**：代码风格统一，减少格式相关 PR 评论
- **工作量**：小

#### 2.8 类型定义（TypeScript 或 JSDoc）

- **现状**：项目纯 JavaScript，无类型检查，API 返回结构、props 类型、composable 返回值均无类型约束
- **目标**：可选方案——① 渐进式引入 TypeScript；② 为核心模块添加 JSDoc 类型注释
- **影响**：开发体验提升，减少运行时类型错误
- **工作量**：大（全量 TS 迁移）/ 中（核心模块 JSDoc）

#### 2.9 单元测试覆盖

- **现状**：无任何单元测试，核心逻辑（`dataExtractor.js`、`http.js`、`moduleModeUtils.js`、composables）无测试保障
- **目标**：为核心工具函数和 composable 添加 Vitest 单元测试，覆盖率目标 60%+
- **影响**：重构安全性提升，回归风险降低
- **工作量**：中

#### 2.10 性能优化

- **现状**：
  - 媒体库网格无虚拟滚动（数据量大时可能卡顿）
  - 图片无懒加载（`el-image` 已内置 lazy 但未启用）
  - 路由已懒加载，但可进一步优化 chunk 分割
- **目标**：启用图片懒加载、媒体库虚拟滚动、分析 bundle 体积优化 chunk 分割
- **影响**：首屏加载速度、滚动流畅度提升
- **工作量**：中

#### 2.11 全局错误边界（ErrorBoundary）

- **现状**：无全局错误捕获，组件渲染错误会导致白屏
- **目标**：添加 Vue `errorHandler` 全局错误处理 + ErrorBoundary 组件包裹路由视图
- **影响**：用户体验提升，错误可追踪
- **工作量**：小

#### 2.12 文档完善

- **现状**：无 README、无组件文档、无 API 文档，新成员上手成本高
- **目标**：编写 README（项目介绍/技术栈/启动方式/目录结构）、核心组件使用文档、API 接口文档
- **影响**：可维护性提升，团队协作效率提升
- **工作量**：中

---

### P3 — 可选（工程化/长期规划）

| # | 事项 | 详情 |
|---|---|---|
| 2.13 | git 提交规范 | 配置 commitlint + husky，统一 commit message 格式 |
| 2.14 | CI/CD | 配置 GitHub Actions / 云效流水线，自动构建、lint、部署 |
| 2.15 | 国际化（i18n） | 如需多语言支持，引入 vue-i18n，抽离硬编码文案 |
| 2.16 | 监控埋点 | 接入前端监控（Sentry/ARMS），收集错误和性能数据 |
| 2.17 | 组件库文档站 | 使用 VitePress 搭建内部组件文档站，展示 ElementorFields 56 个组件用法 |

---

## 三、优化路线图建议

### 第一阶段（1-2 天）— 快速收益

- [x] 2.4 死代码清理（删除 NavigationBar.vue、tailwindcss.config.js）✅
- [ ] 2.6 数据口径统一（customUpload/confirmSelectMedia page 字段）
- [ ] 2.3 token 持久化安全
- [ ] 2.7 添加 lint/format 脚本

### 第二阶段（3-5 天）— 架构一致性

- [ ] 2.1 API 层迁移到新 request() 方式
- [ ] 2.2 globalStore 兼容层退役
- [ ] 2.11 全局错误边界

### 第三阶段（1-2 周）— 质量与体验

- [ ] 2.5 ElementorFields 56 个组件样式统一
- [ ] 2.9 单元测试覆盖（核心模块）
- [ ] 2.10 性能优化（懒加载/虚拟滚动）
- [ ] 2.12 文档完善

### 第四阶段（长期）— 工程化

- [ ] 2.8 类型定义（TS 或 JSDoc）
- [ ] 2.13 git 提交规范
- [ ] 2.14 CI/CD
- [ ] 2.15~2.17 可选优化项

---

## 四、关键指标

| 指标 | 优化前 | 当前 | 目标 |
|---|---|---|---|
| ModuleMode.vue 行数 | 1989 | 328 | ≤ 400 ✅ |
| console.log 残留 | 12+ | 0 | 0 ✅ |
| 乱码文本 | 11+ 处 | 0 | 0 ✅ |
| API 层函数套数 | 3 套 | 2 套（fetchWithAuth + request） | 1 套 |
| Pinia store 数量 | 1 个（global） | 3 个（user/website/global兼容层） | 2 个（user/website） |
| UI 统一页面数 | 0/14 | 14/14 | 14/14 ✅ |
| 单元测试覆盖率 | 0% | 0% | ≥ 60% |
| lint/format 脚本 | 无 | 无（有依赖） | 有 |
